# Runtime dependencies for Analiza Movies
# --------------------------------------
# Core runtime libraries required to run the application
# (CLI analysis + Streamlit dashboard + FastAPI public API)

python-dotenv>=1.0
requests>=2.31
urllib3>=2.0
typing_extensions>=4.9

plexapi>=4.15
pandas>=2.1

# Streamlit and UI stack
streamlit>=1.32
streamlit-aggrid>=0.3.4
altair>=5.2

# FastAPI server stack
fastapi>=0.110
uvicorn[standard]>=0.27

# Optional but strongly recommended: improves reload + removes warning
watchdog>=3.0

# Optional runtime deps (only if you actually use them in the dashboard)
rich>=13.7
plotly>=5.18
sympy>=1.12
graphviz>=0.20